# Core (순수 수학 라이브러리만 의존)
scipy>=1.12.0        # find_peaks, 신호 처리
numpy>=1.26.0        # 선형 대수
pandas>=2.2.0        # 시계열

# Data sources (테스트용)
yfinance>=0.2.40

# Optional: 차트 확인용 (CLI 디버깅)
matplotlib>=3.8.0

# Dev
pytest>=8.0.0
ruff>=0.5.0
